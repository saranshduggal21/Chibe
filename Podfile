# This is a RUBY file!
# This file has to be formatted so that we can actually add the "pod" (3rd Party XCode Library)that we're interested in.

# The "global platform" below defines the MINIMUM iOS version to run the pod on our app (which in this case is 10.0+).
platform :ios, '10.0'

target 'Chibe' do
  use_frameworks!

  # Pods for Chibe
  
    pod 'CLTypingLabel', '~> 0.4.0'
    pod 'IQKeyboardManagerSwift'
    pod 'Firebase/Auth'
    pod 'Firebase/Firestore'
    
end

